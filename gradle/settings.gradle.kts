pluginManagement {
    repositories {
        // maven(url = uri("C:/Users/seroperson/.m2"))
        // mavenLocal()
        // gradlePluginPortal()
    }
}

includeBuild("plugin")
includeBuild("example")
