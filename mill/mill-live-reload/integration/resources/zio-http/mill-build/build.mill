package build

import mill.*, scalalib.*
import mill.meta.MillBuildRootModule

object `package` extends MillBuildRootModule {

  override def mvnDeps = super.mvnDeps() ++ Seq(mvn"me.seroperson::mill-live-reload::${sys.env("PLUGIN_VERSION")}")

}
